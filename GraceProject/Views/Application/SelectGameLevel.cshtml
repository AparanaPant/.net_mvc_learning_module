@model List<GraceProject.Models.AppGameLevel>

@{
    ViewData["Title"] = "Select Game Level";
    Layout = "~/Views/Shared/_StudentLayout.cshtml";
}

<h2>Select Game Level</h2>

<form id="gameLevelForm" asp-controller="Quiz" method="post">
    <select id="gameLevelDropdown" name="gameLevelId" style="width: 100%;" required>
        <option value="">-- Select Game Level --</option>
        @foreach (var level in Model)
        {
            <option value="@level.Id">@level.Name</option>
        }
    </select>

    <br /><br />
    <button type="submit" formaction="@Url.Action("SelectGameLevelPost", "Quiz")" class="btn btn-primary">
        ➕ Continue to Add Quiz
    </button>

    <button type="submit" formaction="@Url.Action("ViewGameLevelQuizzes", "Quiz")" class="btn btn-secondary">
        📄 View Quizzes for Selected Level
    </button>
</form>

<!-- Select2 for searchable dropdown -->
<link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
<script>
    $(document).ready(function () {
        $('#gameLevelDropdown').select2({
            placeholder: "Search game level",
            allowClear: true
        });
    });
</script>
